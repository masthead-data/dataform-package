config {
  type: "table",
  schema: "test",
  description: "Table without reservation assignment (uses default)",
  tags: ["default"]
}

SELECT
  'no_specific_reservation' as reservation_type,
  CURRENT_TIMESTAMP() as created_at,
  'This table should use default reservation or none' as note
